
/*

alert("Soy el profe. Daniel y soy hincha de BOCA");

alert("espero que con el nuevo tecnico ganemos al menos 1 partido");

alert("aqui va el mensaje de texto");

confirm("ud. esta seguro que desea pagar esta deuda ?");
*/

// esto es un comentario de una linea //

let a = 20; // creando un espacio de memoria que se llamará a y almacenará el valor 20.

let b = 30; // aqui creo una variable b y le asigno el valor 30;

let resultado = a + b; 

/* de esta forma estaría creando una tercer 
variable y asignandole el valor que sea
equivalente a la suma de a y b */

console.log(resultado);

let nombreProfesor = "DANIEL MALDONADO";

let domicilioDelNodo = "AV. GUEMES ESQ. VIRGEN DEL VALLE";


/* que es lo QUE NO PUEDO HACER */

    let domicilio_nodo = "OTRA CADENA DE TEXTO"; // No puede haber espacios en blanco


    /* case sensitive quiere decir que es sensible a
    mayusculas y minusculas */

    let miClubFavorito = "CLUB ATLETICO BOCA JUNIORS";

    let MICLUBFAVORITO = "CLUB OTRO";

    let miClubFavoritO = "CLUB SOCIAL 25 DE AGOSTO";

    console.log(miClubFavorito);

    console.log(MICLUBFAVORITO);

    console.log(miClubFavoritO);


    let aa = 50;
    let aA = "DANIEL";

    let bb = 30;

    let resultado2 = aA + bb;

    console.log(resultado2);

    /* primera recomendación. usar
    nombres de variables lo mas
    representativos posibles
    y que tengan una carga semantica
    fuerta */

    let TOTALFACTURA = 0;
    let TotalFactura = 0; // uperCase()
    let totalFactura = 0;

    let totalFacturadoEnEnero = 0; // camelCase

    let tf1 = 0;
    let tf2 = 0;
    let tf3cIVA = 20;

    


