{
// Ejercico 5
let X = 0;

X = Number(prompt("Señor ingrese el valor del numero X"));

     if (X%2 === 0 ) {
        console.log(`es par, su valor es: ${X}`);
     } else{
        console.log(`es impar, su valor es: ${X}`);
     }
     
}